<Window x:Class="InternetTrafficController.Views.MtuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MTU Settings"
        Width="420"
        Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Network Adapter"
                   FontWeight="Bold"/>

        <TextBox x:Name="AdapterTextBox"
                 Grid.Row="2"
                 IsReadOnly="True"
                 Height="30"
                 VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="4">

            <TextBlock Text="MTU Value"
                       FontWeight="Bold"/>

            <TextBox x:Name="MtuTextBox"
                     Height="30"
                     Margin="0,5,0,0"
                     VerticalContentAlignment="Center"/>

            <TextBlock Text="Typical Ethernet/Wi-Fi MTU: 1500"
                       Margin="0,5,0,0"
                       Opacity="0.7"/>

        </StackPanel>

        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Cancel"
                    Width="90"
                    Height="32"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>

            <Button Content="Apply MTU"
                    Width="100"
                    Height="32"
                    Click="Apply_Click"/>

        </StackPanel>

    </Grid>
</Window>